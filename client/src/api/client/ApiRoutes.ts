/**
 * API Routes configuration
 * Centralized endpoint definitions for story generation API
 */

const ApiRoutes = {
  stories: {
    generate: 'generate-story',
    test: 'test',
  },
}

export default ApiRoutes
